<h1>Classifica dei cuochi</h1>
<h4>Tale classifica viene stabilita in base alla media dei voti ottenuti dalle ricette di ogni cuoco!</h4>
<div class="row">
  <%@count=0%>
  <% @cuochi.each do |cuoco| %> 
    <div class="col-md-3">
      <div class="panel panel-default panelcuochi">
       <div class="panel-body" align="center">
        <% if cuoco.avatar? %>
          <%= image_tag cuoco.avatar.url(:medium) , class: "recipe"%>
        <% elsif cuoco.image? %>
          <%= image_tag cuoco.image , class: "recipe"%>
        <%else%>
          <h3>Foto non disponibile!</h3>  
        <% end%>
       </div>
       <div class="panel-footer" align="center">
        <%= @count+=1%>° <%= cuoco.name%><br>
        Numero di ricette: <%=cuoco.recipes.count%>
       </div>
       
      </div>
    </div>
<% end %>
</div>
